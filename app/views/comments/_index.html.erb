<% book.book_comments.each do |book_comment| %>
	<li><%= book_comment.user.name %>：
	<%= book_comment.comment %>

	<% if book_comment.user.id == current_user.id %>
		<%= link_to "削除", book_book_comment_path(book,book_comment), method: :delete %>
	<% end %>
		</li>
<% end %>